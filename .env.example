# Server Port (assigned automatically by Render / Railway)
PORT=3000

# Environment (development or production)
NODE_ENV=production

# AI Dungeon Master Settings (Optional: OpenRouter API key for free AI models)
OPENROUTER_API_KEY=

# Supabase / Database Connection URL (Optional for database persistence)
DATABASE_URL=
